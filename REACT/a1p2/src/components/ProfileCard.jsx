// src/components/ProfileCard.jsx

import React from "react";
import UserCard1 from "./UserCard1";

function ProfileCard({ name, age, location }) {
  return (
    <div>
      <h3>{/* complete the missing code */
       UserCard1({name:"pankaj"})
      }</h3>
      <p>Age: {/* complete the missing code */
      UserCard1({age:28})
      }</p>
      <p>Location: {/* complete the missing code */
      
      
      }</p>
    </div>
  );
}

export default ProfileCard;
